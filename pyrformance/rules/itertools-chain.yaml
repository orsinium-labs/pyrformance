rules:
  - id: itertools-chain
    languages:
      - python
    severity: ERROR
    message: Use `itertools.chain` to loop over multiple iterators.
    pattern-either:
      - pattern: |
          for $ITEM in $X + ...:
            ...
      - pattern: |
          for $ITEM in [..., *$X, ...]:
            ...
